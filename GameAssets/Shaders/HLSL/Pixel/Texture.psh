Texture2D Texture: register(t0);
SamplerState Sampler: register(s0);

struct VertexOut {
    float4 Position: SV_POSITION;
    float2 UV: TEXCOORD0;
};

float4 main(VertexOut vout): SV_TARGET {
    return Texture.Sample(Sampler, float2(vout.UV.x, 1.0 - vout.UV.y));
}
